#addopts= -vs --gherkin-terminal-reporter --showlocals --strict-markers
[pytest]
addopts=--gherkin-terminal-reporter --showlocals --strict-markers
bdd_features_base_dir = features/
markers =
    admin: admin related features
    contact: contact related features
    graphs: graphs related features
    headcount: Maximum headcount per month features
    logs: Logs related features
    olivier: Code tests
    onlyone: Test only this module
    parameters: Parameters related features
    purge: Purge related features
    signin: Sign in  relatedfeatures
    signout: Sign out related features
    usermgnt: User management related features
